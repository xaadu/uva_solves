/*673*/

#include<stdio.h>

int main()
{
    int i,t,f;
    char x[99];
    scanf("%d",&t);
    while(t!=0)
    {
        t--;
        gets(x);
        for(i=0;x[i]!='\0';i++)
        {
            if(x[i]=='(')
               for(j=i;x[j]!='\0';j++)
                    if(x[j])

        }
    }
}

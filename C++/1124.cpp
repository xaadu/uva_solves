/* UVA --> 1124 */

#include <bits/stdc++.h>
using namespace std;

int main()
{
    char x[1000];
    fflush(stdin);
    while(gets(x)){
        cout<<x<<"\n";
        fflush(stdin);
    }
    return 0;
}
